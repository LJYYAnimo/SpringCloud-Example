eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: config
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      profile: private
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest